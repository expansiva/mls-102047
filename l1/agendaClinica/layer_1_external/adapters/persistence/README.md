# Studio harness persistence

The review harness has no tables. The runtime requires a concrete persistence
directory for every declared backend module, so this empty adapter boundary is
kept intentionally.
